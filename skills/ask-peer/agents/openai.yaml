interface:
  display_name: "Ask Peer"
  short_description: "Escalate to Fable for a focused second opinion or review"
  default_prompt: "When you need stronger reasoning, a second opinion, or a scoped review beyond your own, use $ask-peer to escalate to Fable for one focused pass."

policy:
  allow_implicit_invocation: true
