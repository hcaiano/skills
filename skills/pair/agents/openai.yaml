interface:
  display_name: "Pair"
  short_description: "Keep two agents paired, in Herdr or headless"
  default_prompt: "Use $pair to keep a partner agent collaborating with me on this task until I end the pair."

policy:
  allow_implicit_invocation: true
