interface:
  display_name: "Review It"
  short_description: "Grade, simplify, review, and correct a finished change without shipping it"
  default_prompt: "Use $review-it to grade, simplify, and review the current change."

policy:
  allow_implicit_invocation: false
