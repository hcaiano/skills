interface:
  display_name: "Art Director"
  short_description: "Explore and choose a visual direction"
  default_prompt: "Use $art-director to explore, curate, and prove a visual direction before implementation."

policy:
  allow_implicit_invocation: false
