interface:
  display_name: "Art Director"
  short_description: "Direct a long Codex design exploration"
  default_prompt: "Use $art-director to explore many visual directions for one surface."

policy:
  allow_implicit_invocation: false
