interface:
  display_name: "Ship It"
  short_description: "Run the local review gate and ship a PR on demand"
  default_prompt: "Use $ship-it to review and ship the current branch safely."

policy:
  allow_implicit_invocation: false
